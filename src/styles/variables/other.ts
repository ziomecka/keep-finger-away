const standard = {
  standardBorderRadius: 10
};

const button = {
  buttonBorderRadius: standard.standardBorderRadius
};

const select = {
  selectBorderRadius: standard.standardBorderRadius
};

export default {
  ...standard,
  ...button,
  ...select
};