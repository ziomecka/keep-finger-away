import { default as variables } from '../../variables/';
import { ContainerStyle } from '../container/';

export default {
  marginBottom: variables.margins.headingBottom,
  width: '100%',
} as ContainerStyle;
