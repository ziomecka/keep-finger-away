import { default as variables } from '../../variables/';
import { ContainerStyle } from '../container/';

export default {
  backgroundColor: variables.colors.screenBackgroundBasic,
} as ContainerStyle;
