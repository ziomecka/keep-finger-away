import { ContainerStyle } from '../container/';
import { default as variables } from '../../variables/';

export default {
  flex: 0,
  alignItems: 'center',
  margin: variables.margins.contentMargin,
  opacity: 1
} as ContainerStyle;