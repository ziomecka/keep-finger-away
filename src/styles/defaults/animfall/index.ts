import { ViewStyle } from 'react-native';
import { default as animFallContainer } from './animfall.container'

export interface AnimFallStyle {
  animFallContainer?: ViewStyle
};

export default {
  animFallContainer: animFallContainer
} as AnimFallStyle;